// var isNode = false;
// var core;

// if (typeof window === 'undefined') isNode = true

// if (isNode) return module.exports = core = require("./build/core")

// console.log(isNode, window, core)

module.exports = require("./build/core")

//console.log( require('./build/bundle'))

//module.exports = require("./build/bundle")

